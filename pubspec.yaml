name: nested_menu_bar
description: "Nested Menu Bar is a horizontal menu ber for flutter."
version: 1.0.0
homepage:
repository:

environment:
  sdk: ">=3.4.3 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
